<div align="center">

# 图片采集查看器

![Stars](https://img.shields.io/github/stars/Liu-Bot24/page-image-collector?style=flat&label=Stars&cache=20260704) ![Forks](https://img.shields.io/github/forks/Liu-Bot24/page-image-collector?style=flat&label=Forks&cache=20260704) ![Views 14d](https://github-stats.liu-qi.cn/api/badge/Liu-Bot24/page-image-collector/views14d.svg?v=4) ![Clones 14d](https://github-stats.liu-qi.cn/api/badge/Liu-Bot24/page-image-collector/clones14d.svg?v=4) ![Downloads](https://img.shields.io/github/downloads/Liu-Bot24/page-image-collector/total?style=flat&label=Downloads&cache=20260704) ![Release](https://img.shields.io/github/v/release/Liu-Bot24/page-image-collector?style=flat&label=Release&cache=20260704)

![图片采集查看器预览](assets/github-preview.jpg)

Languages: [简体中文](README.md) · [English](README-en.md)

</div>

一款面向日常浏览场景的 Chrome 扩展，用于快速采集网页图片，并提供筛选、查看、批量复制、批量下载与 ZIP 打包能力。

## 文档

- 隐私政策：[PRIVACY.md](./PRIVACY.md)

## 安装

### 从 Chrome 网上应用店安装（推荐）

[图片采集查看器 - Chrome Web Store](https://chromewebstore.google.com/detail/%E5%9B%BE%E7%89%87%E9%87%87%E9%9B%86%E6%9F%A5%E7%9C%8B%E5%99%A8/jchjkamalmegomjijigapilnecdccbom)

### 从 GitHub Releases 安装

1. 进入项目的 `Releases` 页面
2. 下载对应版本的解压包，例如 `page-image-collector-1.2.8-unpacked.zip`
3. 打开 `chrome://extensions/`
4. 开启“开发者模式”
5. 解压下载的 ZIP 文件
6. 点击“加载已解压的扩展程序”
7. 选择解压后的扩展目录

## 1.2.8 更新

- 提升连续扫描、自动采集和页面切换时的状态稳定性，避免旧任务覆盖或清理当前页面的新结果
- 修复单页应用切换页面后仍保留上一页图片的问题，同时保留普通页内锚点跳转的采集状态
- 提升防盗链请求规则的同步与清理稳定性，避免清空、重扫或后台恢复后图片加载规则失效或残留
- 提升批量下载与 ZIP 分卷稳定性，改进后台恢复、图片内容校验和分卷下载衔接
- 完善漫画分页任务的停止机制：关闭漫画模式、清空结果、页面跳转或关闭相关标签页时会及时停止后台分页
- 优化图片元数据更新、格式筛选和长页面自动滚动的状态一致性，减少显示延迟与重复探测

## 1.2.7 更新

- 新增中英文界面：首次安装时会根据浏览器语言自动选择中文或英文，中文与繁体中文环境默认中文，其他语言环境默认英文
- Popup 顶部新增低调的中英文切换入口，用户手动切换后会持久化保存，之后 Popup 与 Workspace 都会沿用用户选择
- 补充 Chrome 原生多语言元数据，让扩展名称和说明可以跟随浏览器语言显示
- 优化英文界面布局，适配英文文案长度，避免顶部按钮、图标和筛选控件出现挤压或异常换行

## 1.2.6 更新

- 优化 Workspace 大批量浏览性能：网格模式在大量图片场景下改为按可视区域渲染，1000+ 图片快速滚动时更流畅
- 保持大图识别能力不退化：虚拟网格下未滚动到的图片也会继续进行最大尺寸、真实格式和高清来源探测
- 改进响应式图片与背景图解析：提升复杂 `srcset`、CSS 背景图、`image-set` 场景下发现大图的稳定性
- 修复部分图片格式显示为 Unknown 的问题：对无扩展名、带格式参数或特殊来源图片，优先使用实际图片内容识别格式
- 改进 Workspace 交互稳定性：优化滚动、缩放、筛选、排序、灯箱、全屏、框选、网格/瀑布流切换时的状态一致性

## 1.2.5 更新

- 提升大图获取与展示一致性：Workspace 会优先使用已发现的最大可用图片，减少网格已识别大尺寸、灯箱或全屏仍停留在缩略图的情况
- 增强动态图片采集能力：补充更多常见懒加载字段、高清图字段、`picture/srcset` 与背景图线索，提升复杂页面中的大图发现率
- 优化漫画分页连续加载：向后加载支持从上次进度继续，临时加载页的自动滚动更稳定，异常中断后可从中断页重试
- 提高 ZIP 打包稳定性：优化大批量 ZIP 的执行流程，并在 Workspace 提供分卷大小设置；Popup 批量 ZIP 会沿用 Workspace 当前设置
- 改进图片格式识别与保存命名：对无扩展名、带格式参数或特殊来源的图片，结合图片内容判断格式，减少 Unknown 和错误扩展名

## 1.2.4 更新

- 修复通用网站场景下“缩略图未升级到原图”的问题，提升原图提取兼容性
- 修复漫画模式分页检测漏判与尾页遗漏问题，提升连续分页获取稳定性

## 1.2.3 更新

- 右键菜单新增「解锁右键」入口，与 Popup 开关状态联动
- Workspace 新增布局模式切换：网格（默认）/ 瀑布流，瀑布流按原始比例完整展示图片

## 1.2.2 更新

- 新增隐藏/显示图片功能：选中图片后可手动隐藏，格式筛选栏新增「隐藏」分类
- 通用防盗链兼容：自动为外部图床设置正确的请求头，支持更多有防盗链机制的网站
- 大图获取增强：改进 HD 规则匹配和 .webp → .jpg 自动回退，全屏查看与灯箱表现一致
- 图片尺寸动态更新：灯箱和全屏成功加载大图后自动更新网格中的尺寸显示
- 分页检测增强：支持图标类翻页按钮识别
- 提高分页检测兼容性，支持更多 WordPress 等站点的分页结构
- 修复漫画模式横幅在基础模式下误显示的问题

## 1.2.0 更新

- 新增漫画模式（实验性）：右键菜单或 Popup 一键进入，图片按原网页阅读顺序排列
- 漫画模式支持自动检测分页，用户确认后可逐页获取后续图片，无需手动翻页
- Popup 新增漫画模式快捷入口
- 优化自动滚动状态同步，停止后会及时通知 Popup 和 Workspace

## 1.1.0 更新

- 新增 Popup 自动滚动入口，适合配合自动采集处理长列表和无限滚动页面
- 优化自动采集、自动滚动、右键解锁的运行时同步与页面刷新后的恢复行为
- 补充批量下载和 ZIP 打包的状态反馈，长任务过程更易观察
- 大批量 ZIP 下载改为自动分卷输出，文件名统一为 `part001.zip` 风格

## 功能特性

- 多来源采集：支持 `img`、`picture`、`srcset`、常见 `data-*` 懒加载字段、背景图等常见图片来源
- 增量采集：支持手动继续扫描与自动采集，会监听常见懒加载字段和动态新增图片，适合信息流、瀑布流、无限滚动页面
- 漫画模式：实验性功能，通过右键菜单或 Popup 入口进入，图片按 DOM 顺序排列，支持自动检测分页，用户确认后逐页加载后续页面图片
  - 分页检测基于页面结构启发式分析，部分非常规分页结构可能无法识别
  - 加载后续分页时，扩展会在每页等待上限内尝试触发基础懒加载，但不承诺所有站点全量无损
- 自动滚动：Popup 提供自动滚动入口，适合配合自动采集处理长列表和无限滚动页面，也可独立使用
- 智能归并：自动规整常见参数差异、尺寸变体与部分原图链接
- 精细筛选：支持格式筛选、分辨率预设、最小短边、最小长边、最小像素阈值
- 双界面工作流：Popup 适合快速操作，Workspace 适合大批量筛选与沉浸式看图；两者共享同一标签页结果集
- 批量处理：支持批量复制链接、逐张批量下载、ZIP 打包下载
- 图片查看：支持普通预览、键盘切换、Workspace 全屏查看
- 来源回溯：支持从采集图片打开来源页面
- 下载反馈：批量下载和 ZIP 打包过程会显示当前进度，便于观察长任务状态
- 本地运行：不依赖外部后端服务，数据保存在浏览器本地环境

## 界面说明

### Popup

适合快速完成以下操作：

- 扫描当前页面
- 快速筛选图片
- 启动自动滚动
- 批量复制链接
- 批量下载
- 打开 Workspace 继续处理
- 进入漫画模式
- 启用页面右键解锁

说明：Popup 属于浏览器扩展弹窗，离开弹窗焦点后会自动关闭。已经启动的自动采集、自动滚动和下载任务仍会继续运行，结果可稍后在 Popup 或 Workspace 中继续查看。

### Workspace

适合处理更复杂的采集任务：

- 大批量浏览与筛选
- 选区式多选
- 更沉浸的预览体验
- 全屏查看与切换
- 批量下载与 ZIP 打包
- 漫画模式：阅读顺序排列、自动分页检测、用户确认后加载
- 布局切换：支持网格与瀑布流两种浏览模式
- 隐藏图片：隐藏状态会在当前采集会话中保存，可通过「隐藏」筛选查看或恢复
- 承接 Popup 已采集结果，继续筛选与处理

## 使用方式

### 手动扫描

- `开始扫描`：建立当前页面的图片结果集
- `继续扫描`：在现有结果基础上增量补充新图片，并更新更优版本
- `清理`：清空当前标签页结果，重新建立新的采集结果集

### 自动采集

- 开启后监听页面新增图片节点，自动增量加入结果集
- 会监听常见懒加载字段和动态新增图片；复杂站点仍可能需要手动继续扫描或配合自动滚动
- 适合动态加载页面，例如信息流、长列表、瀑布流页面
- 建议先手动扫描一次，再开启自动采集

### 自动滚动

- 入口位于 Popup 顶部，位于“自动采集”左侧
- 可单独使用，也可与自动采集配合使用
- 适合需要持续向下加载内容的页面，例如长列表、瀑布流、信息流页面
- 启动后即使 Popup 关闭，当前标签页内的采集任务仍会继续执行
- 采集结果会持续汇总到当前标签页结果集中，之后可直接打开 Workspace 查看和处理

### 筛选能力

- 格式：`JPG / PNG / WebP / GIF / SVG / AVIF / 其他 / 隐藏`
- 分辨率预设：`全部 / ≥720P / ≥1080P / ≥2K / ≥4K`
- 自定义阈值：
  - 最小短边
  - 最小长边
  - 最小像素（MP）
- 常用开关：
  - 采集原图
  - 大到小排序
  - 仅显示竖图
  - 下载转 JPG
  - 批量下载 ZIP
  - 自动滚动（Popup）
  - 自动采集
  - 解锁右键（Popup / 右键菜单）

## 下载与复制

### 单图下载

- 按当前“采集原图”设置选择下载源
- 开启“下载转 JPG”后，会在适用时转换为 JPG 再下载

### 批量下载

- 非 ZIP 模式下，图片会逐张加入浏览器下载队列
- 开启“下载转 JPG”后，会对可转换格式执行 JPG 转换
- 批量任务进行中会显示当前进度状态

### 批量 ZIP 下载

- `批量下载 ZIP` 默认关闭，按需手动开启
- 批量任务会输出 ZIP 文件，适合集中归档
- 大批量任务会自动分卷，并按卷依次下载
- ZIP 分卷设置入口在 Workspace；Popup 发起 ZIP 下载时使用当前全局分卷设置
- 打包与下载过程会显示当前卷和整体处理进度
- 文件名规则：
  - 单卷：`域名-YYYYMMDD-HHMM-images.zip`
  - 分卷：`域名-YYYYMMDD-HHMM-images.part001.zip`
- 自动分卷条件：
  - 单卷约达到 `192MB`
  - 或单卷达到 `300` 张图片
- 大批量任务建议使用默认或更稳的分卷设置；超大分卷会提高内存压力

### 复制策略

- 优先复制图片本体到剪贴板
- 当浏览器权限或格式限制不允许时，自动回退为复制图片链接

## 右键菜单

- 页面空白处（子菜单）：
  - 扫描当前页面图片
  - 扫描当前页面图片（漫画模式）
  - 解锁右键
- 图片元素上（子菜单）：
  - 查看原图
  - 复制原图
  - 下载原图

## 快速开始

1. 打开目标网页
2. 点击扩展图标，进入 Popup
3. 点击 `开始扫描`
4. 按需启用格式筛选、分辨率预设或自定义阈值
5. 根据需要执行以下操作：
   - 批量复制链接
   - 批量下载
   - 打开 Workspace 继续筛选与查看

对于动态页面，建议先执行一次手动扫描，再按需开启自动采集和自动滚动。

## 项目结构

- `manifest.json`：扩展配置（Manifest V3）
- `background/background.js`：消息调度、下载、打包、右键菜单、运行时协调
- `background/stateManager.js`：按标签页管理图片、选中状态与配置状态
- `content/content.js`：页面扫描、自动采集、自动滚动、页面侧能力处理
- `popup/*`：Popup 界面、快速筛选与自动滚动入口
- `workspace/*`：Workspace 界面与交互
- `styles/*`：样式资源

## 权限说明

- `activeTab` / `scripting`：向当前页面注入脚本并执行采集逻辑
- `downloads`：下载单图、批量下载、ZIP 下载
- `contextMenus`：提供右键菜单能力
- `storage`：保存标签页采集状态与界面配置
- `clipboardWrite`：复制图片或图片链接
- `declarativeNetRequest` / `declarativeNetRequestWithHostAccess`：处理部分图床请求兼容
- `host_permissions: <all_urls>`：支持在绝大多数常见网页中执行采集

## 适用范围与限制

- 浏览器受限页面不可注入，例如 `chrome://*`、扩展管理页、Chrome Web Store 等页面
- 纯 Canvas / WebGL 渲染内容通常无法直接提取原图
- 强防盗链、临时签名链接或受限图床可能导致原图链接不可直接下载
- 部分站点需要先展开详情区域或加载更多内容后，才能获得完整图片结果
- 大型动态页面开启自动采集时，可能带来一定性能开销
- 对登录态、强防盗链、脚本延迟加载、内层滚动容器、Canvas / Blob 或虚拟列表页面，扩展只能尽力采集可访问资源
- 后续分页加载保持用户确认，不会在进入漫画模式后自动无限抓取
- Popup 受浏览器扩展弹窗机制限制，离开焦点后会自动关闭；长时间观察、批量筛选和集中处理建议在 Workspace 中完成
- 大批量 ZIP 下载会占用较多浏览器资源，卷数和耗时会随页面图片体积变化

## 版本信息

- 版本：`1.2.8`
- Manifest：`MV3`
